#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success/corrrect)
 */
int main(void)

{
	char alphabet;

	for (alphabet = 122; alphabet >= 97; alphabet--)
	{
		putchar(alphabet);
	}

	putchar('\n');

	retrun (0);

}
